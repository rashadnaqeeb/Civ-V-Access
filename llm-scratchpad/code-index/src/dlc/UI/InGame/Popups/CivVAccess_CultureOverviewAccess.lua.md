# `src/dlc/UI/InGame/Popups/CivVAccess_CultureOverviewAccess.lua`

1364 lines · Accessibility wrapper for the Culture Overview popup (Ctrl+C), presenting a four-tab TabbedShell covering Your Culture, Swap Great Works, Culture Victory, and Player Influence.

## Header comment

```
-- Culture Overview accessibility (Ctrl+C). Wraps the engine popup as a four-tab
-- TabbedShell, every tab a flat BaseMenu list. The screen is small (few cities,
-- few met civs) and the per-row data is rich enough that drill-in is the
-- natural shape; no tab earns a BaseTable. Same reasoning Demographics uses.
--
--   Your Culture     -- per-city Group: name, culture/turn, tourism/turn, GW
--                       filled/total, damage. Drills into the city's GW-
--                       housing buildings (heritage row + any wonders the
--                       city owns), each of which drills into its slots.
--                       Slot Enter runs the move state machine; great-work
--                       details are surfaced via the slot's tooltip.
--                       Antiquity site counts precede the city list.
--   Swap Great Works -- three top-level rows. (1) Your offerings: drills
--                       into three Pulldowns wrapping the engine's per-type
--                       designation widgets; tooltip on each carries the
--                       engine's "other civs may swap for this without
--                       your permission" warning so the trade-asymmetry is
--                       visible to us as it is to sighted players. (2)
--                       Available from other civilizations: drills into
--                       civs with at least one designation, then into each
--                       civ's non-empty slots; activating a leaf queues
--                       that work as the swap target. (3) Trade item:
--                       state-aware label (nothing picked / picked but no
--                       matching designation on your side / ready) that
--                       fires Network.SendSwapGreatWorks on activate when
--                       ready and stays on the tab.
--                       Great-work reading on this tab matches Tab 1: the
--                       work name carries the label (with a type prefix on
--                       foreign leaves to disambiguate within a civ, like
--                       Tab 1's slot index inside a multi-slot building),
--                       and gwTooltip supplies the rich class/artist/
--                       origin/era/yields detail. Pulldown entries fold
--                       both into one announcement because pulldown
--                       entries have no separate tooltip-on-demand path.
--   Culture Victory  -- one Group per met major civ alive, sorted by tourism
--                       descending. Label combines influences/tourism/
--                       ideology/public opinion/excess happiness; drill-in
--                       surfaces the public-opinion and unhappiness tooltips.
--   Player Influence -- perspective picker (defaults to active player) +
--                       tourism-per-turn header + one Group per civ the
--                       perspective player has any influence level on.
--                       Drill-in surfaces influence percent, level tooltip
--                       (with bonuses-at-level callout), modifier breakdown,
--                       trend tooltip, and the bar tooltips (your influence
--                       vs their lifetime culture).
--
-- Initial tab is Your Culture (matches the engine's landing tab).
--
-- Engine integration: ships an override of CultureOverview.lua (verbatim BNW
-- copy + an include for this module). The engine's OnPopupMessage, OnClose,
-- ShowHideHandler, InputHandler, RegisterSortOptions, TabSelect("YourCulture"),
-- and SerialEvent*Dirty wiring stay intact; TabbedShell.install layers our
-- handler on top via priorInput / priorShowHide chains. SerialEventCityInfo-
-- Dirty refreshes Tab 1 (post GW-move slot states); SerialEventGreatWorks-
-- ScreenDirty refreshes Tab 2 (post designate / post swap, including swaps
-- another player initiated against our designations). onShow also calls the
-- engine's RefreshSwappingItems directly so the YourWriting/Art/Artifact
-- pulldowns are populated regardless of which engine tab landed.
```

## Outline

- L82: `local priorInput = InputHandler`
- L83: `local priorShowHide = ShowHideHandler`
- L85: `CultureOverviewAccess = CultureOverviewAccess or {}`
- L95: `local m_gwMoveSource = nil`
- L102: `local m_swapTheirItem = -1`
- L103: `local m_swapTradingPartner = -1`
- L108: `local m_yourCultureTab`
- L109: `local m_swapTab`
- L110: `local m_victoryTab`
- L111: `local m_influenceTab`
- L117: `local buildInfluenceItems`
- L118: `local buildSwapItems`
- L122: `local function activePlayer()`
- L126: `local function activePlayerID()`
- L130: `local function activeTeam()`
- L134: `local function isMP()`
- L138: `local function formatSigned(n)`
- L148: `local function formatNumber(n)`
- L156: `local function civDisplayName(pPlayer)`
- L179: `local function gwCreatorName(gwIndex)`
- L189: `local function eachMetMajorAlive()`
- L209: `local SLOT_TYPE_KEY = { ... }`
- L215: `local function slotTypeLabel(slotType)`
- L226: `local WORK_CLASS_KEY = { ... }`
- L233: `local function workClassLabel(classType)`
- L247: `local function stripArtistTitle(s)`
- L265: `local function gwTooltip(gwIndex)`
- L305: `local HERITAGE_ENTRIES = { ... }`
- L320: `local function resolveHeritageEntry(entry)`
- L348: `local function gwWonderBuildings()`
- L370: `local function cityGwBuildings(city)`
- L390: `local function activateSlotMove(city, buildingClassID, slotIndex, slotType)`
- L452: `local function buildSlotItem(city, buildingClassID, slotIndex, slotType)`
- L477: `local function buildSingleSlotRow(city, building, buildingClass)`
- L510: `local function buildMultiSlotGroup(city, building, buildingClass)`
- L565: `local function buildBuildingGroup(city, building, buildingClass)`
- L576: `local function cityAnnotationPrefix(city)`
- L589: `local function buildCityGroup(city)`
- L650: `local function buildYourCultureItems()`
- L683: `local SWAP_TYPE_WRITING = 3`
- L684: `local SWAP_TYPE_ART = 1`
- L685: `local SWAP_TYPE_ARTIFACT = 2`
- L694: `local SWAP_TYPES = { ... }`
- L724: `local function typeMetaFor(typeId)`
- L733: `local function swappableForTypeId(typeId)`
- L747: `local function gwInlineName(idx)`
- L762: `local function tradeStateLabel()`
- L786: `local function buildOfferingPulldown(typeMeta)`
- L818: `local function buildYourOfferingsGroup()`
- L844: `local function buildForeignOfferingLeaf(gwIndex, ownerID)`
- L870: `local function buildForeignCivGroup(iPlayer)`
- L903: `local function buildForeignOfferingsGroup()`
- L937: `local function buildTradeItem()`
- L959: `buildSwapItems = function()`
- L969: `local function publicOpinionText(opinionType)`
- L985: `local function buildVictoryRowGroup(pPlayer)`
- L1052: `local function buildVictoryItems()`
- L1074: `local INFLUENCE_LEVEL_KEYS = { ... }`
- L1083: `local function influenceLevelText(level)`
- L1094: `local function levelBonusKey(level)`
- L1107: `local function buildInfluenceRowGroup(targetID)`
- L1201: `local function buildPerspectivePulldown()`
- L1219: `buildInfluenceItems = function()`
- L1250: `if type(ContextPtr) == "table" and type(ContextPtr.SetShowHideHandler) == "function" then`
- L1257: `PullDownProbe.installFromControls({ "PlayerPD" }, {}, {}, {})`
- L1271: `TabbedShell.install(ContextPtr, { ... })`
- L1338: `Events.SerialEventCityInfoDirty.Add(function() ... end)`
- L1351: `Events.SerialEventGreatWorksScreenDirty.Add(function() ... end)`
- L1361: `CultureOverviewAccess.formatSigned = formatSigned`
- L1362: `CultureOverviewAccess.formatNumber = formatNumber`
- L1363: `CultureOverviewAccess.civDisplayName = civDisplayName`

## Notes

- L959 `buildSwapItems`: Assigned as a plain function value (not `local function`) because it is forward-declared at L118 and needs to be visible to closures defined earlier in the file.
- L1219 `buildInfluenceItems`: Same forward-declaration pattern as `buildSwapItems`; the perspective pulldown's `onSelected` closure captures it before the body is defined.
- L390 `activateSlotMove`: Runs the two-phase move state machine (mark source, then mark target); pressing the same slot twice cancels the pending source rather than moving.
- L247 `stripArtistTitle`: Strips any trailing parenthesised group from artist names so "(Great Writer)" etc. are not spoken alongside the class word that already conveys the unit type.
